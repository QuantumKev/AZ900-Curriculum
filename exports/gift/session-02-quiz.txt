// Session 2 Quiz — Cloud Benefits, Pricing Models, and IaaS/PaaS/SaaS
// Generated from quizzes/session-02-quiz.md by tools/export-quizzes.py
// GIFT format. Import into Moodle: Question bank > Import > GIFT format.

// objectives: 1.1.6
::S02-Q1:: A workload runs continuously and predictably for the next three years on a specific virtual machine size. Which pricing approach is most likely to reduce its cost? {
    ~Pay-as-you-go
    =A reservation
    ~Azure Spot Virtual Machines
    ~The Azure free account
}

// objectives: 1.1.6
::S02-Q2:: A data-processing job can be interrupted and restarted without harm, and the team wants the lowest possible compute price. Which option fits? {
    ~A reservation
    ~Azure savings plan for compute
    =Azure Spot Virtual Machines
    ~Pay-as-you-go
}

// objectives: 1.1.6
::S02-Q3:: Which statement describes Azure savings plan for compute? {
    ~You commit to a specific VM size and region for one or three years
    =You commit to an hourly spend on eligible compute services for one or three years
    ~You pay nothing until you exceed a monthly threshold
    ~You bid on unused capacity and may be evicted
}

// objectives: 1.1.7
::S02-Q4:: Which statement about serverless computing is correct? {
    ~There are no servers involved anywhere in the architecture
    ~You must keep at least one instance running to receive events
    =An event triggers your code, and resources are allocated only around the run
    ~Serverless is a fourth cloud deployment model alongside public, private, and hybrid
}

// objectives: 1.2.1
::S02-Q5:: Which pair correctly matches the scaling type to its action? {
    ~Vertical scaling adds more virtual machines; horizontal scaling adds CPU and RAM
    =Vertical scaling adds CPU and RAM to a resource; horizontal scaling adds or removes resources
    ~Both add resources; only the billing differs
    ~Vertical scaling happens automatically; horizontal scaling is always manual
}

// objectives: 1.2.2
::S02-Q6:: Which best describes reliability as a cloud benefit? {
    ~Predicting next quarter's cloud spend accurately
    =The ability of a system to recover from failures and continue to function
    ~Guaranteeing identical performance in every region
    ~Encrypting data at rest and in transit
}

// objectives: 1.2.2
::S02-Q7:: A team uses autoscaling and load balancing so response times stay steady as traffic changes. Which benefit does this primarily demonstrate? {
    ~Cost predictability
    =Performance predictability
    ~Manageability in the cloud
    ~Governance
}

// objectives: 1.2.3
::S02-Q8:: Which of these are examples of governance and compliance benefits in the cloud? (Select all that apply.) {
    ~%33.33333%Deploying from templates so resources meet your technical standards
    ~%33.33333%Cloud-based auditing that flags resources out of compliance with your baseline
    ~%-100%Automatic replication of all data to every region by default
    ~%33.33333%Updating resources at scale when standards change
}

// objectives: 1.2.4
::S02-Q9:: Which group of capabilities describes management of the cloud rather than management in the cloud? {
    ~Azure portal, Azure CLI, Azure PowerShell, REST APIs
    =Autoscaling resources, deploying from templates, automatically replacing failing resources, metric-based alerts
    ~Purchasing subscriptions, assigning licenses, paying invoices
    ~Writing application code, running unit tests, publishing releases
}

// objectives: 1.3.1, 1.3.2, 1.3.3, 1.3.4
::S02-Q10:: (Match each scenario to the cloud service type that fits best\: IaaS, PaaS, or SaaS. Each type is used at least once.) {
    =A team lifts and shifts existing servers to the cloud and needs full control of the operating system -> IaaS
    =Developers want a managed platform with built-in scalability so they can focus on application code, without handling OS licensing or patching -> PaaS
    =A finance department subscribes to a ready-to-use expense-tracking application -> SaaS
    =A team needs to rapidly create and tear down test environments that replicate an established server configuration -> IaaS
    =An analytics team wants business intelligence tooling delivered as a service so they can mine their own data -> PaaS
}

// objectives: 1.1.2
::S02-Q11:: A company subscribes to a cloud-based expense-tracking application. Who is responsible for patching the application, and who is responsible for managing user accounts? {
    =The provider patches the application; the customer manages user accounts
    ~The customer patches the application; the provider manages user accounts
    ~The provider does both
    ~The customer does both
}

// objectives: 1.1.5
::S02-Q12:: Which outcome is a direct consequence of the consumption-based model? {
    ~You must forecast capacity a year ahead to avoid penalties
    =You avoid paying for capacity that sits idle
    ~Compute costs are fixed but storage costs vary
    ~Scaling down requires a support request
}
