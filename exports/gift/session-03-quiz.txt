// Session 3 Quiz — Azure Architecture
// Generated from quizzes/session-03-quiz.md by tools/export-quizzes.py
// GIFT format. Import into Moodle: Question bank > Import > GIFT format.

// objectives: 2.1.1, 2.1.2, 2.1.3
::S03-Q1:: (Match each description to the correct term\: datacenter, region, availability zone, region pair, sovereign region.) {
    =A physical facility of racked servers with dedicated power, cooling, and networking, which you do not interact with directly -> datacenter
    =A geographical area containing at least one, and potentially several, nearby datacenters connected by a low-latency network -> region
    =Physically separate datacenters within one region, each with independent power, cooling, and networking -> availability zone
    =Two regions in the same geography, at least 300 miles apart, used for replication and staged platform updates -> region pair
    =An instance of Azure isolated from the main instance of Azure for legal or compliance reasons -> sovereign region
}

// objectives: 2.1.2
::S03-Q2:: Which statement about availability zones is correct? {
    ~Every Azure region supports availability zones
    ~An availability zone is a logical grouping of subscriptions
    =Availability-zone-enabled regions contain a minimum of three separate zones with independent power, cooling, and networking
    ~Availability zones are always in different geographies
}

// objectives: 2.1.1
::S03-Q3:: Your organization must keep replicated data within the same geography for compliance and wants protection against an event that affects an entire region. Which Azure capability addresses this? {
    ~Availability zones
    =Region pairs
    ~Sovereign regions
    ~Resource groups
}

// objectives: 2.1.1
::S03-Q4:: A US government agency requires an instance of Azure that is physically and logically network-isolated from the main instance of Azure. What is this called? {
    ~A private cloud
    =A sovereign region
    ~An availability zone
    ~A dedicated host
}

// objectives: 2.1.4
::S03-Q5:: Which statements about resource groups are correct? (Select all that apply.) {
    ~%-100%A resource can belong to more than one resource group at a time
    ~%33.33333%Every resource must belong to exactly one resource group
    ~%-100%Resource groups can be nested inside other resource groups
    ~%33.33333%Deleting a resource group deletes the resources inside it
    ~%33.33333%Access granted on a resource group applies to the resources it contains
}

// objectives: 2.1.5
::S03-Q6:: In Azure, a subscription acts as which two kinds of boundary? {
    ~A network boundary and a security boundary
    =A billing boundary and an access control boundary
    ~A region boundary and a compliance boundary
    ~A tenant boundary and a licensing boundary
}

// objectives: 2.1.6
::S03-Q7:: An organization has 14 subscriptions across three teams and wants to apply one policy and one role assignment that affect all of them without repeating the work per subscription. What should it use? {
    ~A resource group containing the subscriptions
    ~Tags applied to each subscription
    =A management group containing the subscriptions
    ~A separate Microsoft Entra tenant per team
}

// objectives: 2.1.7
// MANUAL ENTRY NEEDED: S03-Q8 (ordering)

// objectives: 2.1.7
::S03-Q9:: A policy is assigned at the management group level restricting resource deployment to specific regions. What happens to the subscriptions inside that management group? {
    ~Nothing until the policy is assigned again on each subscription
    =They inherit the policy automatically, and resource or subscription owners cannot override it
    ~They inherit the policy but any owner can opt out
    ~Only new subscriptions inherit it; existing ones are unaffected
}

// objectives: 1.1.3
::S03-Q10:: A hospital keeps patient records on infrastructure dedicated to itself and runs its public appointment-booking website on a third-party provider's shared infrastructure. Which cloud model does the combination describe? {
    ~Public cloud
    ~Private cloud
    =Hybrid cloud
    ~Multicloud
}

// objectives: 1.2.1
::S03-Q11:: An application's virtual machines are struggling under load. The team adds four more identical virtual machines behind a load balancer. Which scaling approach is this? {
    ~Vertical scaling
    =Horizontal scaling
    ~Scaling up
    ~Manual failover
}

// objectives: 1.3.4
::S03-Q12:: A team must run software that requires a specific operating system version, custom kernel settings, and its own installed agents. Which cloud service type fits, and why? {
    ~SaaS, because it requires the least technical expertise
    ~PaaS, because the provider handles the operating system
    =IaaS, because it gives control over the operating system and installed software
    ~Any of the three; the requirement does not affect the choice
}
