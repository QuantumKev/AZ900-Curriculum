// Session 4 Quiz — Azure Compute
// Generated from quizzes/session-04-quiz.md by tools/export-quizzes.py
// GIFT format. Import into Moodle: Question bank > Import > GIFT format.

// objectives: 2.2.1
::S04-Q1:: A team needs to run multiple instances of a lightweight application on a single host, starting and stopping instances quickly, without managing a separate operating system for each instance. Which compute option fits best? {
    ~Virtual machines
    =Containers
    ~Azure Virtual Desktop
    ~Availability sets
}

// objectives: 2.2.1
::S04-Q2:: Which statement correctly distinguishes Azure Container Instances from Azure Kubernetes Service? {
    ~Container Instances orchestrates the lifecycle of a fleet of containers; AKS runs a single container
    =Container Instances is the fastest, simplest way to run a container in Azure; AKS is a container orchestration service for managing fleets
    ~Container Instances runs only Windows containers; AKS runs only Linux containers
    ~Container Instances is IaaS; AKS is SaaS
}

// objectives: 2.2.2
::S04-Q3:: A retailer wants identical, load-balanced virtual machines that increase in number automatically during traffic spikes and decrease afterward. Which option should they use? {
    ~An availability set
    =A virtual machine scale set
    ~Azure Virtual Desktop
    ~A single larger virtual machine
}

// objectives: 2.2.2
::S04-Q4:: What do update domains and fault domains belong to? {
    ~Availability zones
    ~Region pairs
    =Availability sets
    ~Virtual network subnets
}

// objectives: 2.2.2
::S04-Q5:: A company needs to give 200 distributed contractors access to a standardized Windows desktop and a fixed set of applications, managed centrally, with data staying in Azure rather than on personal laptops. Which service fits best? {
    =Azure Virtual Desktop
    ~Virtual machine scale sets
    ~Azure App Service
    ~Azure Container Apps
}

// objectives: 2.2.3
::S04-Q6:: Which of these must you select or configure when you create an Azure virtual machine? (Select all that apply.) {
    ~%33.33333%Size (family, vCPU count, and RAM)
    ~%33.33333%Storage disks
    ~%33.33333%Virtual network and network interface
    ~%-100%A Log Analytics query
    ~%-100%An Azure Policy initiative
}

// objectives: 2.2.2
::S04-Q7:: In the VM name `D2s_v5`, what does the `D` indicate? {
    ~The number of data disks attached
    =The VM family, in this case general purpose
    ~The datacenter the VM runs in
    ~That the VM is deallocated
}

// objectives: 2.2.4
::S04-Q8:: A development team wants to host a web application and a REST API in the language of their choice, with automatic scaling, built-in load balancing, and deployment from a Git repository — without managing operating systems. Which service fits best? {
    ~Azure Virtual Machines
    =Azure App Service
    ~Azure Virtual Desktop
    ~Azure Data Box
}

// objectives: 2.2.1, 1.1.7
::S04-Q9:: Which statement about Azure Functions is correct? {
    ~Functions must run continuously to be able to receive events
    ~Functions are stateful by default, and Durable Functions makes them stateless
    =An event triggers the function, Azure runs the code, then deallocates resources, and you are charged for the CPU time used while it runs
    ~Functions can only be triggered by an HTTP request
}

// objectives: 1.1.7
::S04-Q10:: Which requirement most strongly suggests a serverless approach rather than a virtual machine? {
    ~A line-of-business application that must run 24 hours a day at steady load
    =Short tasks triggered by messages that arrive unpredictably, finishing in seconds
    ~A legacy application that needs a specific OS build and custom drivers
    ~A standardized desktop image for remote workers
}

// objectives: 1.3.1
::S04-Q11:: Which scenario is the clearest example of IaaS? {
    ~Subscribing to a hosted email service
    =Deploying virtual machines you patch and configure yourself
    ~Using a managed analytics service with built-in tooling
    ~Using an event-triggered function service
}

// objectives: 2.1.4
::S04-Q12:: You want to delete a temporary development environment consisting of a VM, its disk, its network interface, and a public IP address, all created together. What is the cleanest way to remove everything? {
    ~Delete the virtual machine; the dependent resources are always removed with it
    =Delete the resource group that contains them
    ~Apply a ReadOnly lock, then delete the subscription
    ~Move the resources to a second resource group, then delete the original
}
