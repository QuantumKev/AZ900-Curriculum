// Session 5 Quiz — Azure Networking
// Generated from quizzes/session-05-quiz.md by tools/export-quizzes.py
// GIFT format. Import into Moodle: Question bank > Import > GIFT format.

// objectives: 2.2.5
::S05-Q1:: What is the primary purpose of an Azure virtual network? {
    ~To store unstructured data accessible over HTTP
    =To let Azure resources communicate with each other, with the internet, and with on-premises networks
    ~To replicate data across region pairs
    ~To assign role-based permissions to users
}

// objectives: 2.2.5
::S05-Q2:: A team needs two virtual networks in different Azure regions to communicate privately, with traffic staying on the Microsoft backbone rather than crossing the public internet. What should they configure? {
    ~A site-to-site VPN
    =Virtual network peering
    ~A private endpoint
    ~Azure DNS
}

// objectives: 2.2.5
::S05-Q3:: An employee working from a home office needs an encrypted connection from their laptop into an Azure virtual network. Which connectivity option is this? {
    ~Site-to-site VPN
    =Point-to-site VPN
    ~ExpressRoute
    ~Virtual network peering
}

// objectives: 2.2.5
::S05-Q4:: An organization requires a dedicated private connection between its datacenter and Azure that does not travel over the public internet, with predictable latency and high throughput. Which option meets this? {
    ~Site-to-site VPN over the internet
    =Azure ExpressRoute
    ~Point-to-site VPN
    ~Network security groups
}

// objectives: 2.2.5
::S05-Q5:: Which statements about Azure VPN Gateway are correct? (Select all that apply.) {
    ~%25.0%You can deploy only one VPN gateway per virtual network
    ~%25.0%One VPN gateway can connect to multiple locations
    ~%25.0%VPN gateways are deployed by default as two instances in an active/standby configuration
    ~%-100%VPN gateway traffic is unencrypted because it uses the Microsoft backbone
    ~%25.0%A VPN gateway can serve as a failover path for an ExpressRoute connection
}

// objectives: 2.2.5
::S05-Q6:: Which statement about Azure DNS is correct? {
    ~You can purchase domain names directly in Azure DNS
    =Azure DNS hosts DNS domains and provides name resolution, and supports private DNS zones for virtual networks
    ~Azure DNS replaces the need for public IP addresses
    ~Azure DNS is only available in sovereign regions
}

// objectives: 2.2.6
::S05-Q7:: What is the defining characteristic of a private endpoint? {
    ~It has a public IP address reachable from anywhere in the world
    =It exists within a virtual network and uses a private IP address from that network's address space
    ~It encrypts all traffic between two virtual networks
    ~It is a firewall rule applied at the subnet level
}

// objectives: 2.2.6
::S05-Q8i:: A public endpoint has a public IP address and can be accessed from anywhere in the world. {T}

::S05-Q8ii:: A private endpoint uses a private IP address from the virtual network's address space. {T}

::S05-Q8iii:: Adding a private endpoint automatically removes all public access to every resource in the subscription. {F}

// objectives: 2.2.5
::S05-Q9:: A team wants to restrict inbound traffic to a subnet so that only HTTPS from a specific address range is allowed. Which capability of Azure virtual networking handles this? {
    ~Virtual network peering
    =Network security groups
    ~Azure DNS alias records
    ~ExpressRoute Global Reach
}

// objectives: 1.1.4
::S05-Q10:: An organization wants to keep a regulated database on infrastructure it controls while running a public-facing application in Azure, with the two connected. Which cloud model is this, and which Azure connectivity option would support it? {
    ~Public cloud; virtual network peering
    =Hybrid cloud; site-to-site VPN or ExpressRoute
    ~Private cloud; point-to-site VPN
    ~Multicloud; Azure DNS
}

// objectives: 2.1.1
::S05-Q11:: A design requires that a workload survive the loss of one datacenter inside a single Azure region. Which capability should the design use? {
    ~Region pairs
    =Availability zones
    ~Sovereign regions
    ~Virtual network peering
}

// objectives: 2.2.2
::S05-Q12:: Which option provides resiliency for a group of virtual machines against a single hardware failure or planned maintenance event within a region, at no additional charge for the grouping itself? {
    =An availability set
    ~A second subscription
    ~A public endpoint
    ~Azure Data Box
}
